import React from 'react';
import { AIChatbot } from './AIChatbot';

// This component was restored to prevent breaking the Builder.io project.
// It likely contained additional logic or styling, but is restored here as a simple wrapper.

export function EnhancedAIChatbot() {
  return <AIChatbot />;
}
