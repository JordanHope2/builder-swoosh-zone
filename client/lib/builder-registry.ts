import { Builder } from '@builder.io/sdk';
import { JobSearch } from '../components/JobSearch';

Builder.registerComponent(JobSearch, {
  name: 'JobSearch',
  inputs: [], // No inputs for this component for now
});
